# Core (always needed)
pandas>=2.0
numpy>=1.24

# Config module
pydantic>=2.0
pydantic-settings>=2.0

# Dev / test
pytest>=7.0

# Optional — install per use case:
# pyspark>=3.4          # for spark_data_quality.py
# mistralai             # for llm_client.MistralProvider
# anthropic             # for llm_client.AnthropicProvider
# google-generativeai   # for llm_client.GeminiProvider
# openai                # for llm_client.OpenAIProvider
